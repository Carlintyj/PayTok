/**
 * A component for displaying payment page UI. (Transfer to merchant / Other people)
 * @component
 * @returns {JSX.Element} Payment Page UI.
 */
export default function PaymentPage() {
  return (
    <div>
      <h1>Payment Page</h1>
    </div>
  );
}
