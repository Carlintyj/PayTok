/**
 * A component for displaying home page UI. (Display $ balance/ Recent Transactions)
 * @component
 * @returns {JSX.Element} Login Page UI.
 */
export default function HomePage() {
  return (
    <div>
      <h1>Home Page</h1>
    </div>
  );
}
