/**
 * A component for displaying forgot password page UI.
 * @component
 * @returns {JSX.Element} Forgot Password Page UI.
 */
export default function ForgotPassword() {
  return (
    <div>
      <h1>Forgot Password Page</h1>
    </div>
  );
}
