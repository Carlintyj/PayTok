/**
 * A component for displaying transactions page UI. (Top up / Withdraw/ Transfer/ Payment/ History)
 * @component
 * @returns {JSX.Element} Transactions Page UI.
 */
export default function Transactions() {
  return (
    <div>
      <h1>Transactions Page</h1>
    </div>
  );
}
